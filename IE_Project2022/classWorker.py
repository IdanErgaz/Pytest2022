class worker:
    def __int__(self,first,last,role):
        self.first=first
        self.last=last
        self.role = role
        # print('hi, I am a worker, my fname is:{}, lname:{}, role:{}'.format(self.fname, self.lname, self.role))
#Main
newWorker = worker('Idan', 'Ergaz', 'Manager')